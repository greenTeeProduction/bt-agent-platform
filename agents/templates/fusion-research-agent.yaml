name: fusion-research-agent
description: Multi-model Fusion deliberation agent — parallel panel analysis, judge comparison, final synthesis
version: "1.0.0"
tree: fusion
schedule: ""
inputs:
  - name: task
    type: text
    required: true
outputs:
  - name: final_answer
    type: markdown
  - name: fusion_analysis
    type: json
quality:
  min_length: 300
  required_sections: ["Consensus", "Contradictions", "Blind Spots"]
metadata:
  category: hermes-builtin
  tags: "fusion,research,multi-model,deliberation,openrouter"
